#!/usr/bin/env zsh

export HOMEBREW_GITHUB_API_TOKEN=$(cat $HOME/.intercom_github_api_token | tr -d '\n')
export INTERCOM_USER=aaron.chambers

#Hammer (local Intercom CLI tool) config
alias assume-role='function(){eval $(hammer assume-role $@);}'
